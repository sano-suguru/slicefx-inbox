## Local secrets (never commit)
.env

## .NET
bin/
obj/
*.user
.vs/
.vscode/
*.suo

## Spin WASI publish output
src/Inbox.Server/dist/

## Spin local runtime state
src/Inbox.Server/.spin/

## Spin CLI binary and associated files (downloaded locally, not project artifacts)
spin
spin.sig
crt.pem
LICENSE
